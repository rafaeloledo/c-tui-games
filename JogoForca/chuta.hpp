#pragma once
#include <map>
#include <array>

namespace Forca {
	void chuta(std::map<char, bool>& chutou, std::array<char, 5>& chutes_errados, int& num_chutes_errados, std::string& palavra_secreta);
}